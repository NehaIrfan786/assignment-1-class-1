function makeSandwich(...items: string[]) {
    console.log(`Making a sandwich with ${items.join(',')}.`);
} 

makeSandwich('ham', 'cheese', 'lecture');
makeSandwich('turkey', 'bacon', 'avocado');
makeSandwich('peanut butter', 'jelly'); 